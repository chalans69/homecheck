const form=document.querySelector('#analysis-form');
if(form){form.addEventListener('submit',()=>{const box=document.querySelector('#progress');box.hidden=false;box.scrollIntoView({behavior:'smooth'});const steps=['Localisation','Trajets','Risques','Urbanisme','Nuisances','Marché immobilier','Synthèse'];let i=0;const timer=setInterval(()=>{i=Math.min(i+1,steps.length-1);document.querySelector('#progress-text').textContent=steps[i];document.querySelector('#progress-bar').style.width=((i+1)/steps.length*100)+'%';if(i===steps.length-1)clearInterval(timer)},550)})}
if(window.HOMECHECK){const d=window.HOMECHECK,m=L.map('map');L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(m);const h=[d.home.lat,d.home.lon],w=[d.work.lat,d.work.lon];L.marker(h).addTo(m).bindPopup('Bien').openPopup();L.marker(w).addTo(m).bindPopup('Travail');let layer;if(d.route)layer=L.geoJSON(d.route,{style:{color:'#26695c',weight:5}}).addTo(m);const group=L.featureGroup([L.marker(h),L.marker(w),...(layer?[layer]:[])]);m.fitBounds(group.getBounds().pad(.15));}

